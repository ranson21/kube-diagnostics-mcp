# Create in every namespace that runs probes, with the SAME value as the hub's probe-token.
apiVersion: v1
kind: Secret
metadata:
  name: kube-diagnostics-probe-token
type: Opaque
stringData:
  probe-token: REPLACE_WITH_RANDOM_PROBE_TOKEN
